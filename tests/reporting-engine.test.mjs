import test from "node:test";
import assert from "node:assert/strict";
import {buildReport} from "../lib/reporting-engine.mjs";
test("excludes unapproved measures from clinical evidence",()=>{const r=buildReport({patients:[{id:"p"}],metrics:[{patient_id:"p",review_state:"pending"}],intakes:[],journeys:[],tasks:[],appointments:[],outcomes:[],reportedOutcomes:[],messages:[],commercial:[]});assert.equal(r.clinical.approvedMetricCount,0);assert.equal(r.clinical.baselineCoverage,0)});
test("calculates program funnel with explicit denominators",()=>{const r=buildReport({patients:[{id:"p1"},{id:"p2"}],intakes:[{patient_id:"p1",checked_in_at:"x"}],journeys:[{patient_id:"p1",current_state:"PROGRAM_ACTIVE"},{patient_id:"p2",current_state:"REGISTERED"}],tasks:[],appointments:[],metrics:[],outcomes:[],reportedOutcomes:[],messages:[],commercial:[]});assert.equal(r.commercial.assessmentConversion,50);assert.equal(r.commercial.programConversion,100)});
test("keeps clinical and commercial measures separate",()=>{const r=buildReport({patients:[],intakes:[],journeys:[],tasks:[],appointments:[],metrics:[],outcomes:[],reportedOutcomes:[],messages:[],commercial:[{event_type:"revenue",amount_minor:700000},{event_type:"marketing_spend",amount_minor:100000}]});assert.equal(r.commercial.roas,7);assert.equal(r.clinical.approvedMetricCount,0)});
test("requires active marketing consent for eligible stories",()=>{const base={patients:[{id:"p"}],intakes:[],journeys:[],tasks:[],appointments:[],metrics:[],reportedOutcomes:[],messages:[],commercial:[],outcomes:[{patient_id:"p",completed_at:"2026-09-19"}]};assert.equal(buildReport({...base,marketingConsents:[]}).readiness.storiesEligible,0);assert.equal(buildReport({...base,marketingConsents:[{patient_id:"p",granted:1,withdrawn_at:null}]}).readiness.storiesEligible,1)});
